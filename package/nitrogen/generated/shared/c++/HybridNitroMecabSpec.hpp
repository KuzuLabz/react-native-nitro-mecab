///
/// HybridNitroMecabSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `DictionaryInfo` to properly resolve imports.
namespace margelo::nitro::nitromecab { struct DictionaryInfo; }
// Forward declaration of `Node` to properly resolve imports.
namespace margelo::nitro::nitromecab { struct Node; }

#include <NitroModules/Promise.hpp>
#include <string>
#include <vector>
#include <optional>
#include "DictionaryInfo.hpp"
#include "Node.hpp"

namespace margelo::nitro::nitromecab {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NitroMecab`
   * Inherit this class to create instances of `HybridNitroMecabSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNitroMecab: public HybridNitroMecabSpec {
   * public:
   *   HybridNitroMecab(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNitroMecabSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNitroMecabSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNitroMecabSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual bool getIsInitialized() = 0;
      virtual std::shared_ptr<Promise<void>> initialize(const std::string& dictUri, const std::optional<std::vector<std::string>>& userDictUris) = 0;
      virtual void deinitialize() = 0;
      virtual std::vector<DictionaryInfo> getDictionaryInfo() = 0;
      virtual std::string getVersion() = 0;
      virtual std::string tokenize(const std::string& text) = 0;
      virtual std::vector<std::string> wakati(const std::string& text) = 0;
      virtual std::vector<Node> parseToNode(const std::string& text) = 0;
      virtual std::shared_ptr<Promise<std::string>> getLattice(const std::string& text, double nBest) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NitroMecab";
  };

} // namespace margelo::nitro::nitromecab
